import unittest

#from linked_lists import creation_of_list, append, print_list, delete, tmp_list, refresh, search, sortedMerge, mergeSort, getMiddle, sortInverse
from linked_lists import Node_of_list, LinkedList
from Dictionaries import data_base, data_type, ascending_data, descending_data

#----------------- Class to test the linked list ------------------

class TestLinkedList(unittest.TestCase):

    def test_creation(self):
        #self.assertEqual(0, 1)
        self.assertEqual(0, 0)

    def test_append(self):
        self.assertEqual(0,0)


#----------------- Class to test the Dictionary ------------------

class TestDictionary(unittest.TestCase):

    def 